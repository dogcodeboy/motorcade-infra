# Deployment Log — 2026-01-21

## Entries
- (prepared) PLAT_05_nginx_reverse_proxy_leadgen_api.yml added to repo. After running on prod, replace this line with an entry including verification outputs:
  - `nginx -t` success
  - `curl -i http://127.0.0.1/lead/health` returns 200
  - `curl -i http://127.0.0.1/api/lead/health` returns 200
  - backup path printed (if config previously existed)
