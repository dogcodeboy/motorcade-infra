# LEADGEN_07B — NoBackslash ParseStable overwrite

This checkpoint replaces LEADGEN_07B playbook with a parse-stable version that avoids YAML backslash escape churn and validates persistence by request_id OR email.
