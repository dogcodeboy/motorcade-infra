## Files changed

- `ansible/playbooks/LEADGEN_07B_wave3_psycopg_driver_fix_and_persistence_verify.yml`
  - Parse-stable regex patterns in requirements driver detection (`psycopg`/`psycopg2`).
  - Parse-stable intake JSON parse guard.
  - Persistence polling checks `app.leads` by **request_id OR email**.

- `docs/RUNBOOK.md`
  - Appended this checkpoint note.
