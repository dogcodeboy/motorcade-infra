# NEXT STEPS (after PLAT_02C)

Proceed to PLAT_02D — Postgres Hardening (still no schema):
- systemd resource controls (CPU/Memory)
- log collection + rotation
- local backup playbook hooks (no S3 yet)
- explicit firewall rules (if needed)
- readiness + liveness checks formalized
