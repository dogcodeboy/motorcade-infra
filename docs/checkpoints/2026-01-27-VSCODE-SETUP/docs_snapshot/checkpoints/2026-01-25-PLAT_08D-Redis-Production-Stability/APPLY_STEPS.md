# Apply Steps (merge)

From repo root:

```bash
cd ~/Repos/motorcade-infra
unzip ~/Downloads/motorcade-infra_CHECKPOINT_2026-01-25_PLAT_08D_DOCS_ONLY_MERGE.zip
```

Then review and commit:

```bash
git status
# Expected: docs/RUNBOOK.md modified, docs/RESOURCES.md added, new checkpoint folder added
```

Commit message (GitKraken):
- `CHECKPOINT 2026-01-25 — PLAT_08D Redis stability verified (docs-only)`
