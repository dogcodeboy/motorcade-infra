SESSION HANDOFF — motorcade-infra — LEADGEN_07B (RepoRoot autodetect + parse-stable)

If playbook still returns 0|0 persistence after this change, next step is to confirm LeadGen queue/worker mode (health output) and DB target/env vars.
