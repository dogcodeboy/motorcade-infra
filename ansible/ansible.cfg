[defaults]
inventory = inventory
host_key_checking = False
retry_files_enabled = False
stdout_callback = default

# NOTE: The "yaml" stdout callback is not guaranteed to be available on all
# Ansible installations (commonly missing on minimal/RHEL setups). Using the
# built-in "default" callback keeps playbook runs deterministic across hosts.

[ssh_connection]
pipelining = True
